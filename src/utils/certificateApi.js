/*import axios from 'axios';

const certificateApi = axios.create({
  baseURL: /*'/api/certificateApi',*//*'/certificates',
  withCredentials: true, // if you send cookies / JWT
});

export default certificateApi;*/
